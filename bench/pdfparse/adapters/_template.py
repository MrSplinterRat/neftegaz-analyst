"""ОБРАЗЕЦ АДАПТЕРА. Скопируй в adapters/<имя>.py и заполни parse().

Весь договор — двадцать строк ниже. Больше от кандидата ничего не нужно:
стенд сам найдёт файл, сам сверит с эталоном и сам назовёт сдвиг сдвигом.

    NAME    — как назвать кандидата в отчёте.
    parse() — принимает путь к PDF, возвращает список таблиц.

Форма таблицы:

    {
        "caption": "Table 3c. World Petroleum and Other Liquid Fuels Production …",
        "columns": ["2025Q1", "2025Q2", …, "2027", ],   # см. ★ниже
        "rows": [
            {"label": "OPEC members subject to OPEC+ agreements (d)",
             "values": ["21.55", "21.96", …]},
        ],
    }

★ЗНАЧЕНИЯ — СТРОКАМИ, ДОСЛОВНО, И ПРОЧЕРК ТОЖЕ ЗНАЧЕНИЕ. Не приводи к float,
не выбрасывай пустые ячейки, не заменяй прочерк на None и не убирай его вовсе.
Прочерк в этих отчётах значит «данных нет» и занимает СВОЙ столбец; выбросишь —
весь остаток строки уедет влево, и это будет выглядеть правдоподобно. Стенд
ловит такой сдвиг отдельным контролем именно потому, что глазами он не виден.

★ШАПКА ДВУХЪЯРУСНАЯ. На странице над кварталами стоит полоса годов:

        2025          |      2026         |      2027        |    Year
    Q1  Q2  Q3  Q4    |  Q1  Q2  Q3  Q4   |  Q1  Q2  Q3  Q4  | 2025 2026 2027

Верни склеенные обозначения — "2025Q1", "2025Q2" … — если разборщик отдаёт оба
яруса. Если отдаёт только нижний, верни его как есть: стенд различает эти два
исхода и не считает второй провалом. Плоский текст не даёт связи «какой это Q1»
в принципе, и ради этой связи всё и затевается.

Заголовок таблицы сверяется по вхождению первых сорока знаков, поэтому лишний
колонтитул в caption не страшен.

Не установлен нужный пакет — не беда: стенд напишет «не загрузился» и пойдёт
дальше, остальные кандидаты отработают.
"""

from __future__ import annotations

NAME = "образец"
SABOTEUR = False


def parse(path: str) -> list[dict]:
    raise NotImplementedError("скопируй файл и заполни")
